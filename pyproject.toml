[build-system]
build-backend = "uv_build"
requires = ["uv-build>=0.8.5,<0.12"]

[project]
name = "ecc-fe"
version = "0.1.0-alpha.0"
requires-python = ">=3.11"
dependencies = [
  "click>=8.1",
  "typer>=0.12",
]

[dependency-groups]
packaging = [
  "pyinstaller==6.17.0",
  "pyinstaller-hooks-contrib==2026.6",
]
test = [
  "pytest==8.4.2",
]

[project.scripts]
ecc-fe = "fecompiler.cli.main:main"
fecompiler = "fecompiler.cli.main:main"

[tool.uv]
build-backend.module-name = ["fecompiler"]
build-backend.module-root = ""
build-backend.source-exclude = ["fecompiler/thirdparty/**"]

[tool.pytest.ini_options]
testpaths = ["test"]
norecursedirs = ["bazel-bin", "bazel-out", "bazel-ecc-fe", "bazel-testlogs", ".venv"]
